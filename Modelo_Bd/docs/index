
CREATE INDEX idx_usuario_perfil_ativo ON supabase.usuario(perfil, ativo);

CREATE INDEX idx_paciente_nome ON supabase.paciente(nome);
CREATE INDEX idx_paciente_ativo ON supabase.paciente(ativo);

CREATE INDEX idx_vinculo_usuario_paciente ON supabase.vinculo(usuario_id, paciente_id);

CREATE INDEX idx_evolucao_prontuario_data ON supabase.evolucao(prontuario_id, criado_em DESC);
CREATE INDEX idx_evolucao_autor ON supabase.evolucao(autor_id);


CREATE INDEX idx_alergia_prontuario ON supabase.alergia(prontuario_id);

CREATE INDEX idx_exame_prontuario_status ON supabase.exame(prontuario_id, status);

CREATE INDEX idx_mensagem_chat ON supabase.mensagem(remetente_id, paciente_id, enviado_em DESC);